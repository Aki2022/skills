---
date: YYYY-MM-DD
summary: 一文で症状を述べる。本文を開かずに一覧・トリアージできる粒度にする
skills: []
repo: ""
canon: xxxxxxxx
paths: []
---

<!--
配置: <保管ルート>/entries/YYYY-MM/YYYY-MM-DD-<slug>.md
新規 Write のみで完結させる。既存ファイルを読まない（並行セッションで競合しないため）。

frontmatter:
  date    必須。時系列の絞り込みに使う
  summary 必須。一文
  skills  必須（空配列可）。skill を使っていない場面のトラブルは [] のままでよい
  repo    任意。集計軸
  canon   必須。そのとき読んでいた横断正典の版。取り方:
            readlink ~/.agents/AGENTS.md | sed 's|.*/nix/store/\([a-z0-9]\{8\}\).*|\1|'
          「新しい規律を読んだ上での違反か」を後から判定するための唯一の手掛かり。
          セッション開始時刻との突き合わせが不要になる。
  paths   必須（空配列可）。トラブルの対象ファイル。hook 化の可否
          （検知条件を機械的に書けるか）を判断する材料。本文に散らばらせない

secret は書く時点で値をマスクし構造を残す（Bearer <redacted> 等）。
会社ドメイン・顧客名・ローカルパス・リポジトリ名はマスクしない
（どこで再発しているかを追えなくなると集積の目的が消える）。
-->

## 意図 — 何をしようとしていたか

<!-- hook を書くときの「誤検知の境界」になる。正常な作業との差を書く -->

## 実際にやったこと — 実行したコマンド・書いたコードを実文で

<!--
要約しない。実文をそのまま貼る。
hook の検知条件は「観測可能な行動」に対してしか書けず、要約からは導けない。
実文が必要なのは「厚く書く」ためではなく、検知条件の材料だから。
-->

```
（コマンド / コード / 出した報告文をそのまま）
```

## 観測された壊れ方 — ログ / エラーを実文で。実害を含む

<!-- 実害は具体的に（例: 約9分の重複デプロイ / 提案が pending のまま残った / 全ファイルが素通り） -->

```
（ログ / エラー出力をそのまま）
```

## 気づいた経緯 — 誰が / 何がどうやって気づいたか

<!--
検知器の有無を示し、hook 化の優先度に直結する。
人間が手で数えて気づいた = 検知器が存在しない、は最優先の材料。
-->

## 既に存在していた正解 — 具体パス。「無かった」「不明」も可

<!--
「無かった」と「不明」を書き分ける。
無かった → 規律ではなく機能欠落。形式化先が変わる。
不明     → 調べていない。曖昧に埋めると分析を誤誘導する。
-->

## 本来どうすべきだったか（仮説）

<!-- 仮説として書く。「わからない」と書いてよい -->

## なぜそうしなかったか（仮説）

<!--
知らなかった      → skill / docs の不足
知っていて飛ばした → hook による機械強制
わからない        → そう書く。断定形は分析を誤誘導する
-->
